# Backend local (PocketBase) — pivote a GitHub-as-backend, pero queda por si vuelves
backend/pocketbase.exe
backend/pb_data/

# Editor / herramientas
.vscode/
.claude/settings.local.json

# Dependencias y lockfiles (instalación local de herramientas de build)
node_modules/
package.json
package-lock.json
pnpm-lock.yaml
yarn.lock
